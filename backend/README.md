# Backend

This directory will contain the backend service (Node.js + TypeScript).

Suggested stack: NestJS or Express with TypeORM/Prisma, Socket.IO for realtime, Redis for caching.

Plan
- Create REST API for auth, profiles, swipes, matches, messages
- Socket.IO server for real-time events
- Database migrations and entities (Postgres)
